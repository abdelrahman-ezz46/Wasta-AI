{
  "name": "@wasta/coach-card",
  "version": "1.0.0",
  "private": true,
  "description": "AI Career Coach results-page card. Drop into the host app's frontend and mount below the existing score breakdown / static feedback.",
  "main": "index.ts",
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
