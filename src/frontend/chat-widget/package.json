{
  "name": "@wasta/chat-widget",
  "version": "1.0.0",
  "private": true,
  "description": "Floating support chatbot widget. Drop into the site/app layout so it's available on every page.",
  "main": "index.ts",
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
